# Minimal .htaccess for Osmo Resource Pages
# Only essential directives to avoid server errors

# Enable basic CORS if supported
<IfModule mod_headers.c>
    Header set Access-Control-Allow-Origin "*"
</IfModule>

